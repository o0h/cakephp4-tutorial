# o0h/cakephp4-tutorial

## 基本的な使い方
WIP / zenn.devにて詳しい資料を作成中です

### ローカルでの開発を始める
次のコマンドを実行することで初期化とサービスの起動を行います
```sh
make
```
ブラウザから `http://localhost:8080/` にアクセスして、画面が表示される事を確認してください

もし、初期化だけを行いたい場合は次のコマンドを実行します
```sh
make init
```

2回目以降(初期化が不要な場合)には、次のコマンドを打つとサービスの起動のみを行います
```sh
make up
```

次のコマンドでサービスを終了させることができます
```sh
make down
```
